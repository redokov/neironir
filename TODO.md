# TODO

- [x] **Админка** — веб-интерфейс для управления системой:
  - [x] Счётчик обработанных документов (всего / за период)
  - [x] Просмотр feedback по каждому документу (какие PII найдены, какие правки внёс пользователь)
  - [x] Кнопка «Запустить дообучение» — запуск `opf train` на накопленных аннотациях
  - [x] Статус дообучения — прогресс, текущая эпоха, loss, когда завершится
  - [x] Возможность утверждать/отклонять предложенные правила (теперь через UI + API)
- [x] **Конвертация .docx → .md** — чекбокс «Результат в MD-формате» при загрузке.
  Pandoc используется для сохранения структуры документа (заголовки,
  списки, выделение), с fallback на плоский текст через python-docx.
- [x] **Применение пользовательских правок к итоговому файлу** — кнопка
  «Сохранить правки в файл» в review-секции. Эндпоинт
  `POST /api/v1/documents/{id}/apply-feedback` транслирует offsets
  правок из исходного текста в координаты очищенного файла и
  переписывает `result.{md|docx}` с сохранением сквозной нумерации
  плейсхолдеров.